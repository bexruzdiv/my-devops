---
- name: "Pull postgres-exporter image"
  docker_image:
    name: "{{ postgres_exporter.image }}"
    source: pull
  tags:
    - install

- name: Prepare folders
  file: path="{{ item.path }}" state=directory mode="{{ item.mode }}" owner="{{ item.owner }}"
  with_items:
    - { "path": "/var/log/docker", "owner": "root", "mode": "0755" }
    - {
        "path": "/var/log/docker/postgres-exporter",
        "owner": "root",
        "mode": "0777",
      }

- name: "Install Systemd service for postgres-exporter"
  template:
    src: docker_service_unit.j2
    dest: "/etc/systemd/system/postgres-exporter.service"
    owner: root
    group: root
    mode: "0644"
  tags:
    - install

- name: Reread systemd configs
  systemd:
    daemon_reload: yes
  tags:
    - install

- name: "Enable and start postgres-exporter service"
  systemd:
    name: "postgres-exporter"
    enabled: yes
    state: restarted
  tags:
    - enable
    - install
